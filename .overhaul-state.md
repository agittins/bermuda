# État de l'overhaul — bermuda (ha-integration-overhaul)

> Fichier d'état durable pour reprise de session. Supprimé au commit final Phase 9.

## Progression

| Phase | État | Commit |
|-------|------|--------|
| 0 — Préflight | ✓ 2026-07-09 | (pas de commit — tag backup) |
| 0.5 — État de l'art web | ✓ 2026-07-09 | — |
| 1 — Découverte | ✓ 2026-07-09 | (commit de ce fichier) |
| 2 — Architecture | ✓ 2026-07-09 | (commit de cette passe) |
| 3 — Refactor structurel | ✓ 2026-07-09 | (commit de cette passe) |
| 4 — Client externe (bluetooth) | ☐ | — |
| 5 — Entités | ☐ | — |
| 6 — Config flow & translations | ☐ | — |
| 7 — Qualité transverse | ☐ | — |
| 8 — Tests ≥ 90 % | ☐ | — |
| 9 — Validation finale | ☐ | — |

## Contexte établi (Phase 0)

- **Repo** : fork canonique de `agittins/bermuda` (releases autonomes 0.11→0.13 via release-please). Pas de remote upstream local → PAS de rebase upstream.
- **Branche de travail** : `refactor/integration-overhaul-2026-07-09` (créée depuis main b2b5afe)
- **Tag de backup** : `backup/before-overhaul-20260709-210813`
- **HA dev** : container docker `homeassistant`, HA **2026.7.0**, config dans `/home/stephane/homeassistant/config/` (≠ chemin par défaut du skill : les custom_components sont dans `config/custom_components/`)
- **Config entry bermuda sur HA dev** : entry_id `01KFMQ1CR5TG3TB0FW7G4CYRWR`, **version 2.1**, unique_id None, source user, options : configured_devices / exclude_devices / track_categories
- **Anomalie relevée** : `manifest.json` version = 0.10.0 alors que la dernière release GitHub = v0.13.0 (release-please ne bump pas le manifest → à corriger)
- **Note docker** : `docker logs --since 8h` ne retourne rien (bug horloge ?) → utiliser `--since 3m` ou `--tail`.

## Baseline perf (Phase 1.5.bis)

- Setup du domain (boots précédents, HA 2026.7) : `Setup of domain bermuda took 0.00 seconds`
- **Baseline restart 2026-07-09 21:11:57 (HA 2026.7.0)** : `Setup of domain bermuda took 0.00 seconds` → cible Phase 9.2.bis : rester à 0.00s (< 1s)
- importtime à froid dans le container : `custom_components.bermuda` cumulé **1 615 681 µs (~1.62s)**, dont `homeassistant.components.bluetooth` 960 859 µs et `custom_components.bermuda.coordinator` 982 589 µs (cumulatif). En conditions réelles HA, bluetooth est déjà chargé — la part propre à bermuda est faible.

## Inventaire unique_id (Phase 1.5) — GELÉ, AUCUN CHANGEMENT AUTORISÉ

Filet durable : `tests/test_unique_id_regression.py` (324 l.) gèle tous les patterns ; ARCHITECTURE.md documente « do NOT change without migration ». **Décision Phase 5.1.bis : ne PAS changer les unique_id** (format fonctionnel, shippé, sans collision).

13 patterns d'entités (où `{mac}` = `BermudaDevice.unique_id`, adresse normalisée) :
1. `{mac}` — device_tracker, sensor area (base), BermudaEntity base
2. `{mac}_floor` · 3. `{mac}_scanner` · 4. `{mac}_rssi` · 5. `{mac}_range`
6. `{mac}_{scanner_wifi_or_ble_mac}_range` · 7. `…_range_raw` (legacy wifi MAC, ESPHome 2025.3)
8. `{mac}_area_switch_reason` · 9. `{mac}_area_last_seen` · 10. `{mac}_micro_location`
11. `{mac}_in100_vcc` / `_in100_temperature` / `_in100_adc_voltage`
12. `{mac}_ref_power` (number) · 13. `{mac}_mobility` (select)
+ 5 littéraux globaux `BERMUDA_GLOBAL_{PROXY_COUNT,ACTIVE_PROXY_COUNT,DEVICE_COUNT,VISIBLE_DEVICE_COUNT,NEARBY_DEVICES}`
+ formes d'adresse spéciales alimentant `{mac}` : MAC simple, `{uuid}_{major}_{minor}` (iBeacon), IRK 32-hex, scanner = wifi MAC registry (bermuda_device_scanner.py:246, LOAD-BEARING)
+ config entry unique_id = `bermuda` (single instance) ; subentries unique_id = scanner/addr

## Inventaire Phase 1 (résumé décisionnel)

- **35 .py intégration (8 554 l.) + 44 .py tests (10 099 l., ~584 tests, cov déclarée 99,5 %, gate fail_under=95)**
- Plateformes : sensor, device_tracker, number, select. iot_class calculated, type device, requirements [].
- Coordinator : `BermudaDataUpdateCoordinator` + 3 mixins (scanners/metadevices/microlocation), état dans `coordinator.devices`, tick 1.05 s + push adverts.
- Quality Scale : **Gold atteint dans le code**, Platinum quasi (gaps = 8 règles docs-* + `quality_scale` non déclaré au manifest).
- Dette structurelle prioritaire : coordinator.py 880 l. (constructeur 167 l., update loop 133 l.) ; bermuda_device.py 702 l. (2 parseurs binaires embarqués iBeacon/IN100 + side-effect IRK) ; bermuda_advert.py 554 l. ; options_flow.py 463 l. avec logique métier dupliquée ; 8 fonctions > 100 l. ; constantes de tuning éclatées sur 4 conventions (héritage des 4 forks sources).
- ~20 anomalies notées (rapport complet dans la conversation) ; top : distance_filter.py:70-75 cast float sur None possible ; :96 `or` avale 0.0 → DISTANCE_INFINITE ; manifest version 0.10.0 ≠ release 0.13.0 ; bermuda_irk.py:191 constructeur 11 args positionnels ; dépendance API privée `bluetooth.api._get_manager` ; el.json 3 strings non traduites + intents EN hardcodé ; requirements.txt pollué (pyudev, pyserial, aioesphomeapi… non requis par le manifest).
- Features fork à préserver : IRK enrolment, micro-locations/MCP, mobility+hysteresis, area presence overrides, IN100, catégories ESPresense, nearby-devices, trackers signatures, subentries+migration v1→v2, SecretFilter.
- ⚠ Env pytest local cassé (site-packages user 3.14 + habluetooth incompatible → ImportError) — À RÉPARER en préalable de Phase 2 ; CI GitHub verte fait foi en attendant.

## État de l'art HA backend (Phase 0.5 — résumé)

Document complet : `scratchpad/ha-backend-state-of-art-2026-07-09.md` (sourcé, avec table Quality Scale et breaking changes 2026.5→7).

**Déjà conforme (vérifié dans le code)** : OptionsFlow sans setter config_entry ; pas d'anti-pattern listener+reload (dépréciation 2026.6, erreur dure 2026.12 — `reload_on_update=False` + un seul update listener) ; `runtime_data` ; `async_forward_entry_setups` pluriel ; `entry.async_on_unload` ; config subentries (`subentry_flow.py`) ; pas de dépréciations device_tracker 2026.4-6 ; EntityDescription frozen/kw_only à spot-checker.

**Actions réelles issues de la veille** :
1. Bump `hacs.json` min HA : 2025.8 → 2026.6/2026.7
2. Évaluer `BaseScannerEntity` + `in_zones` + `tracking_type` (HA 2026.6, pensé pour les trackers BLE — multi-zones)
3. Bluetooth scanning mode « Auto » par défaut depuis 2026.6 (proxies ESPHome/Shelly auto-migrés Active→Auto) — valider cadence RSSI/précision
4. Python floor : HA core exige ≥ 3.14.2 (cycle 2026.4+) ; `[tool.mypy] python_version` 3.13 → 3.14
5. Gap Quality Scale = 100 % documentation (7 règles docs-* Gold ouvertes dans `quality_scale.yaml`) ; hassfest jamais passé sur quality_scale.yaml → valider tôt

## Décisions validées par l'utilisateur (2026-07-09)

1. **Périmètre : overhaul complet** (phases 2→9, refactor structurel inclus, filets = suites de régression existantes).
2. **BaseScannerEntity/in_zones (HA 2026.6)** : évaluer en Phase 5, adopter SEULEMENT si comportement device_tracker préservé (entity_id + unique_id inchangés) ; sinon documenter et rester TrackerEntity.
3. **Structure cible Phase 3 : découpage ciblé à plat** (validée) :
   - coordinator.py ~400 l. : extraire dump_devices/redaction (glue diagnostics), _apply_area_entity_overrides → area_entity.py, factoriser les 5 méthodes `*_created`
   - bermuda_device.py ~450 l. : extraire parsers iBeacon+IN100 → beacon_parsers.py, classification adresse → address_type.py
   - bermuda_advert.py ~350 l. : extraire filtre RSSI + lissage → rssi_filters.py
   - options_flow.py UI pure : ranking/filtrage devices → helpers.py
   - const.py : TOUTES les constantes de tuning, convention unique
   - repairs.py NOUVEAU (RepairsFlow pour scanner_without_area)
   - le reste inchangé (mixins, sensor_*, entity.py, trilateration.py, trackers.py…)

## Décisions prises par Claude (documentées)

- **unique_id : aucun changement** (Phase 5.1.bis, cas « format fonctionnel + shippé » → stabilité prime ; gelé par test de régression).
- Pas de rebase upstream (fork canonique).

## Phase 2 — contenu de la passe (2026-07-09)

Constat : architecture déjà conforme (Debouncer, config_entry lié au coordinator, background tasks trackées, listeners via async_on_unload, first_refresh, unload propre — le DataUpdateCoordinator s'auto-shutdown via l'entry). Corrections apportées :
- coordinator.py : suppression `self.platforms` (mort), commentaire PLR1730 orphelin, défensivités obsolètes `hasattr(entry, "options")` / `getattr(entry, "subentries", {})` → accès directs, `except KeyError: pass` silencieux de handle_devreg_changes → `.get()` explicite
- entity.py : BermudaGlobalEntity.device_info enrichi (manufacturer, entry_type=SERVICE)
- device_tracker.py : suppression try/except ImportError BaseTrackerEntity pré-2024.9 → import top-level `homeassistant.components.device_tracker` (le chemin config_entry est un alias DÉPRÉCIÉ retiré en 2027.6, découvert au deploy ; top-level nécessite `# type: ignore[attr-defined]` car pas dans __all__)
- manifest.json : version 0.10.0 → 0.13.0 (resync release) + release-please-config.json : extra-files en format json/jsonpath (cause racine du manifest jamais bumpé)
- hacs.json : min HA 2025.8 → 2026.6
- pyproject.toml : ruff target py312→py314, mypy python_version 3.13→3.14 (aligné HA core ≥3.14.2)
- Env local : `.venv` reconstruit (uv, Python 3.14.6, requirements_test.txt + requirements_dev.txt) — pytest et mypy opérationnels localement
- Décision scanning mode : `BluetoothScanningMode.ACTIVE` du register_callback conservé (le changement 2026.6 « Auto » concerne les adaptateurs, pas le paramètre du callback ; zéro bénéfice à changer, risque cadence RSSI)

Vérifs : ruff ✓, mypy --strict ✓ (35 fichiers), pytest ✓ (~584 tests, cov 99,51 %)

## Phase 3 — contenu de la passe (2026-07-09)

Nouveaux modules (structure « découpage ciblé à plat » validée) :
- `beacon_parsers.py` : parse_ibeacon()/parse_in100() purs (dataclasses IBeaconParse/In100Telemetry) — extraits de bermuda_device. Changement de comportement documenté : un frame 0x02 < 22 octets est désormais ignoré entièrement (avant : register_ibeacon_source avec beacon_unique_id "None_None_None").
- `address_type.py` : classify_address() pur (regex compilées, mapping MSBs→type) — side-effects IRK/PBLE restés dans le device.
- `rssi_filters.py` : RssiFilterPolicy (STATIONARY/MOVING), clamp_outlier/ema/rssi_dispersion purs, constantes MAD_TO_SIGMA/OUTLIER_SIGMA_FACTOR nommées — extraits de bermuda_advert.
- `helpers.py` : is_device_selectable() (dédoublonne scan/selectdevices de l'options flow).
- `repairs.py` : NOUVEAU RepairsFlow pour scanner_without_area (issue passée is_fixable=True + data ; confirm → coordinator.refresh_scanners(force=True), nouvelle méthode publique du mixin qui reset le mémo _scanners_without_areas). strings.json : fix_flow.step.confirm ajouté (EN seul → parité langues en Phase 6).

Bugfixes :
- distance_filter.peak_retreat_velocity : guard None sur old_distance (TypeError runtime possible, gap auto-documenté) — cast retiré.
- distance_filter.minimum_hugging_average : `or` → `is not None` (0.0 n'est plus mappé sur DISTANCE_INFINITE) — characterization test mis à jour en connaissance de cause.

Coordinator allégé : _apply_area_entity_overrides → area_entity.apply_area_entity_overrides() (wrapper 1 ligne conservé pour l'API tests) ; 5 méthodes *_created factorisées via _mark_platform_created (API publique conservée) ; flag mort _seed_configured_devices_done supprimé ; retour vestigial _async_gather_advert_data → None ; défensivités getattr(device_config) supprimées (mocks tests complétés avec device_config={}).

Constantes centralisées (const.py) : MANUFACTURER_ID_APPLE, RSSI_BLUEZ_BOGUS, DEVICE_ACTIVE_AGE, CYCLE_TIME_WARN/ERROR, DEBOUNCE_COOLDOWN, PICKER_STALE_AGE (60*60*2 dupliqué), SCANNER_AGE_FRESH/WORRY, OPT_RSSI_OFFSET_MIN/MAX, OPT_MAX_DEVTRACK_TIMEOUT ; _EPSILON dans trilateration ; DISTANCE_INFINITE remplace 9999 dans set_ref_power.

Déviations documentées vs plan : service_dump_devices reste dans coordinator (délègue déjà à redaction.py — 2e indirection sans valeur) ; sensortext/__str__ de AreaTests non factorisés (formats divergents par nature) ; paramètres algorithmiques de trilateration laissés en contexte (commentés sur place, une partie déjà dans const.py section AREA_*).

Code mort supprimé : blocs commentés tx_power (bermuda_advert), `# if not …seed…`, `# if nearest_scanner`, commentaire PLR1730 orphelin. suppress(Exception) options_flow → try/except loggé.

Vérifs : ruff ✓, mypy --strict ✓ (40 fichiers), suite complète ✓ cov 99,02 % (repairs.py couvert en Phase 8).

## TODO restants

- Phase 3 : deploy_and_restart en cours → logs 75 s → commit
- Notes pour phases suivantes :
  - Phase 3 : seed-bodge `_seed_configured_devices_done` (flag écrit jamais lu), retour vestigial `_async_gather_advert_data() -> bool`, Debouncer cooldown=1.0 magic, 5 méthodes `*_created`, split coordinator/device/advert selon structure validée
  - Phase 5 : duplications unique_id (BermudaSensor/BermudaEntity, device_tracker), `async_add_devices(entities, False)` positionnel, évaluation BaseScannerEntity/in_zones, `state` override direct dans device_tracker
  - Phase 8 : test d'ancrage pattern reload (add_update_listener + reload_on_update=False, dépréciation 2026.12)
